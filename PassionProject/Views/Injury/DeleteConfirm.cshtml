@model PassionProject.Models.InjuryDto

@{
    ViewBag.Title = "View";
}

<h2>View</h2>

<h3>Are you sure you want to delete this injury?</h3>
<div>

    <dl class="dl-horizontal">

        <dt>
            @Html.DisplayNameFor(model => model.InjuryName)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.InjuryName)
        </dd>

    </dl>

    @using (Html.BeginForm("Delete", "Injury", new { id = Model.InjuryID }))
    {
        @Html.AntiForgeryToken()

        <div class="form-actions no-color">
            <input type="submit" value="Delete" class="btn btn-default" /> |
            @Html.ActionLink("Back to List", "List")
        </div>
    }
</div>
